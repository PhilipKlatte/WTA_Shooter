class RectangularHitZone extends RectangularZone{
    constructor(fromX, fromY, untilX, untilY) {
        super();
        this.fromX = fromX;
        this.fromY = fromY;
        this.untilX = untilX;
        this.untilY = untilY;
    }
}