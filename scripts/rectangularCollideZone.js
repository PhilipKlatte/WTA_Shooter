class RectangularCollideZone {
    constructor(fromX, fromY, untilX, untilY) {
        this.fromX = fromX;
        this.fromY = fromY;
        this.untilX = untilX;
        this.untilY = untilY;
    }
}